import { BellingAndShippInfo } from './belling-and-shipp-info';

describe('BellingAndShippInfo', () => {
  it('should create an instance', () => {
    expect(new BellingAndShippInfo()).toBeTruthy();
  });
});
